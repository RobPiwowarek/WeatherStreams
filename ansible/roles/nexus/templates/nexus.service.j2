[Unit]
Description=nexus service
After=network.target

[Service]
Type=forking
ExecStart=/var/{{ nexus_version }}/bin/nexus start
ExecStop=/var/{{ nexus_version }}/bin/nexus stop
User=root
Restart=on-abort

[Install]
WantedBy=multi-user.target
